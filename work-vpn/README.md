TBC

2. Add a tiny README note (optional but wise)

In README.md or work-vpn/README.md, add 3–4 lines max:
	•	What the script does
	•	That DNS is scoped via /etc/resolver/*.example
	•	That real resolver files are local-only and ignored
	•	“If DNS breaks, reboot is no longer required” (the real win)

This stops you six months from now going:

why the hell did I do it this way?