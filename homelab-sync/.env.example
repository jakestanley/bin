# homelab-sync
#
# Copy to: homelab-sync/.env
#
# Notes:
# - Values are intentionally simple `KEY="value"` assignments so the same file can be used by:
#   - macOS bash script: homelab-sync.sh
#   - Windows PowerShell port: homelab-sync.ps1

# Directory that contains the homelab repos (usually the current directory).
HOMELAB_SYNC_ROOT="."

# Space-separated repo names under HOMELAB_SYNC_ROOT.
HOMELAB_SYNC_REQUIRED_REPOS="homelab-infra homelab-standards"
HOMELAB_SYNC_OPTIONAL_REPOS="homelab-edge"

# Git remote name to sync against.
HOMELAB_SYNC_GIT_REMOTE="origin"

# Path to the imports sync script inside each repo.
HOMELAB_SYNC_IMPORTS_SCRIPT="scripts/sync_imports.py"

# Venv directories to prefer if present (first match wins).
# - macOS/Linux: <venv>/bin/python
# - Windows:     <venv>\Scripts\python.exe
HOMELAB_SYNC_VENV_DIR_CANDIDATES=".venv venv"

# Python executable name/path used when no venv is found.
# Examples:
# - macOS: python3
# - Windows: python
HOMELAB_SYNC_PYTHON="python3"
